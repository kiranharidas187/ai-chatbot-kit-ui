/**
 * Single import point for icons so the icon library can be swapped without
 * touching components. Add icons here as components need them.
 */
export {
  Send as SendIcon,
  Bot as BotIcon,
} from 'lucide-react';
